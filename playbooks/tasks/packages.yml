---
- name: Update and upgrade apt packages
  become: true
  apt:
    upgrade: yes
    update_cache: yes
    cache_valid_time: 86400 #One day

- name: Install Packages for all
  become: true
  apt:
    pkg:
    - git
    - hddtemp
    - htop
    - openssh-server
    - vim
    - yadm
